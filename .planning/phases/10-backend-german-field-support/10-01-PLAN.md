---
phase: 10-backend-german-field-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/controllers/adminClientCreditorController.js
autonomous: true

must_haves:
  truths:
    - "PUT request with glaeubiger_name field saves value to creditor document in MongoDB"
    - "PUT request with forderungbetrag field persists the value correctly"
    - "All 10 German fields (glaeubiger_name, glaeubiger_adresse, glaeubigervertreter_name, glaeubigervertreter_adresse, forderungbetrag, email_glaeubiger, email_glaeubiger_vertreter, dokumenttyp, needs_manual_review, review_reasons) are accepted and saved without error"
    - "Existing PUT requests using old field names (sender_name, sender_email, etc.) continue to work identically"
  artifacts:
    - path: "server/controllers/adminClientCreditorController.js"
      provides: "updateCreditor handler accepting both German and English field names"
      contains: "glaeubiger_name"
  key_links:
    - from: "server/controllers/adminClientCreditorController.js"
      to: "MongoDB creditor document"
      via: "Object.assign on creditor + client.save()"
      pattern: "glaeubiger_name.*forderungbetrag.*dokumenttyp"
---

<objective>
Extend the updateCreditor controller to accept and persist all 10 German field names used by the Glaubiger-Tabelle, while maintaining full backward compatibility with existing English field names.

Purpose: The frontend Glaubiger-Tabelle displays and will edit creditors using German field names (glaeubiger_name, forderungbetrag, etc.). The current PUT endpoint only accepts English field names (sender_name, claim_amount, etc.). Without this change, inline editing in Phase 11 will silently discard German field values.

Output: Updated adminClientCreditorController.js where updateCreditor accepts both field name conventions.
</objective>

<execution_context>
@/Users/luka.s/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luka.s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@server/controllers/adminClientCreditorController.js
@server/models/Client.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend updateCreditor to accept and persist German field names</name>
  <files>server/controllers/adminClientCreditorController.js</files>
  <action>
In the `updateCreditor` method (line ~290), extend the destructuring from `req.body` to also extract all 10 German fields:
- `glaeubiger_name`, `glaeubiger_adresse`, `glaeubigervertreter_name`, `glaeubigervertreter_adresse`
- `forderungbetrag`, `email_glaeubiger`, `email_glaeubiger_vertreter`
- `dokumenttyp`, `needs_manual_review`, `review_reasons`

In the `Object.assign` block (line ~345), add these German fields alongside the existing English fields. Apply the same defensive pattern used for English fields:
- String fields: `field?.trim() || originalCreditor.field || ''` — but ONLY update if the field was actually sent (check `field !== undefined`) to avoid overwriting existing values with empty strings when only one convention is used
- `forderungbetrag`: String field (raw formatted amount like "1.876,40 EUR"), use same pattern as other strings
- `needs_manual_review`: Boolean field, use same pattern as `is_representative` — `field !== undefined ? field : originalCreditor.field`
- `review_reasons`: Array field — if sent and is an array, use the sent value; otherwise keep original
- `dokumenttyp`: String field, same pattern as other strings

IMPORTANT: When a German field is sent, do NOT also overwrite its English counterpart or vice versa. Each field name convention writes independently to the Mongoose schema. The schema already defines both sets of fields (lines 82-99 in Client.js). The frontend reads with fallback chains like `c.glaeubiger_name || c.sender_name`, so both can coexist.

For the status history metadata `changes` object (line ~371), add tracking for German field changes too:
- `german_fields_updated`: true if any German field was included in the request body

Do NOT modify the addCreditor, deleteCreditor, getCreditors, or any other method — only updateCreditor.
  </action>
  <verify>
1. Read the modified file and confirm all 10 German fields appear in the destructuring AND in the Object.assign block
2. Confirm old English fields (sender_name, sender_email, etc.) are still present and unchanged in the Object.assign block
3. Run `node -e "require('./server/controllers/adminClientCreditorController.js')"` from project root to confirm no syntax errors
  </verify>
  <done>
- All 10 German fields are destructured from req.body
- All 10 German fields are persisted via Object.assign with defensive defaults
- Old English field handling is completely unchanged (backward compatible)
- No syntax errors in the module
  </done>
</task>

</tasks>

<verification>
1. `node -e "require('./server/controllers/adminClientCreditorController.js')"` exits without error
2. The updateCreditor method destructures all 10 German fields from req.body
3. The Object.assign block includes all 10 German fields with appropriate fallback patterns
4. The old English fields (sender_name, sender_email, sender_address, reference_number, claim_amount, is_representative, actual_creditor) are still handled identically to before
5. No other methods (addCreditor, deleteCreditor, getCreditors) were modified
</verification>

<success_criteria>
- PUT /clients/:clientId/creditors/:creditorId accepts glaeubiger_name and saves it to MongoDB
- PUT /clients/:clientId/creditors/:creditorId accepts forderungbetrag and saves it to MongoDB
- All 10 German fields (glaeubiger_name, glaeubiger_adresse, glaeubigervertreter_name, glaeubigervertreter_adresse, forderungbetrag, email_glaeubiger, email_glaeubiger_vertreter, dokumenttyp, needs_manual_review, review_reasons) are accepted without error
- Existing PUT requests with old field names (sender_name, claim_amount, etc.) continue working identically
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-german-field-support/10-01-SUMMARY.md`
</output>
