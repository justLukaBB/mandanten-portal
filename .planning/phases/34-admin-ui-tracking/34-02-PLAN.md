---
phase: 34-admin-ui-tracking
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - MandantenPortalDesign/src/app/components/client-detail.tsx
  - MandantenPortalDesign/src/app/components/overview-sections.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-04

must_haves:
  truths:
    - "Client Detail overview shows '2. Anschreiben starten' button that is clickable when status is IDLE and disabled when PENDING/FORM_SUBMITTED/SENT"
    - "Clicking the trigger button when IDLE opens a ConfirmActionDialog before making the API call"
    - "Client Detail overview shows SecondLetterStatusBadge with countdown when IDLE, 'Wartet auf Formular' when PENDING, 'Formular eingereicht' when FORM_SUBMITTED, 'Gesendet' when SENT"
    - "Admin can override plan type via Select control when status is FORM_SUBMITTED — select is hidden otherwise"
    - "A 'Jetzt senden' button appears when status is FORM_SUBMITTED and triggers the send-second-letter endpoint"
    - "Trigger mutation handles alreadyTriggered response (HTTP 200, success:false) with info toast instead of error"
  artifacts:
    - path: "MandantenPortalDesign/src/app/components/client-detail.tsx"
      provides: "2. Anschreiben section with trigger button, status badge, plan override, send button"
      contains: "SecondLetterSection"
  key_links:
    - from: "MandantenPortalDesign/src/app/components/client-detail.tsx"
      to: "MandantenPortalDesign/src/store/api/clientDetailApi.ts"
      via: "useTriggerSecondLetterMutation, useSendSecondLetterMutation, useOverrideSecondLetterPlanTypeMutation hooks"
      pattern: "useTriggerSecondLetterMutation|useSendSecondLetterMutation|useOverrideSecondLetterPlanTypeMutation"
    - from: "MandantenPortalDesign/src/app/components/client-detail.tsx"
      to: "MandantenPortalDesign/src/app/components/confirm-action-dialog.tsx"
      via: "ConfirmActionDialog for trigger and send confirmation"
      pattern: "ConfirmActionDialog"
---

<objective>
Add the 2. Anschreiben section to Client Detail overview with trigger button, status badge, plan type override select, and send button.

Purpose: Admin needs full visibility and control of the 2. Anschreiben workflow from the client detail view — trigger, monitor status, override plan type, and send.
Output: Updated client-detail.tsx with SecondLetterSection component
</objective>

<execution_context>
@/Users/luka.s/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luka.s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-admin-ui-tracking/34-RESEARCH.md
@.planning/phases/34-admin-ui-tracking/34-01-SUMMARY.md
@MandantenPortalDesign/src/app/components/client-detail.tsx
@MandantenPortalDesign/src/app/components/confirm-action-dialog.tsx
@MandantenPortalDesign/src/store/api/clientDetailApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SecondLetterSection to Client Detail overview</name>
  <files>
    MandantenPortalDesign/src/app/components/client-detail.tsx
  </files>
  <action>
Add a `SecondLetterSection` component inside client-detail.tsx (or as a self-contained section rendered in `renderOverview()`). Place it AFTER the `QuickActions` section and BEFORE `PhasePrerequisites` in the renderOverview function.

**Imports to add at the top of client-detail.tsx:**
- `useTriggerSecondLetterMutation, useSendSecondLetterMutation, useOverrideSecondLetterPlanTypeMutation` from `../../store/api/clientDetailApi`
- `Send` from `lucide-react` (for the section icon)
- `toast` from `sonner` (if not already imported)

**SecondLetterSection component design:**

The section is a white card (`#FFFFFF` bg, `1px solid #E5E7EB` border, `12px` radius, `20px 24px` padding) with these elements:

1. **Section header row:**
   - Left: Section label "2. ANSCHREIBEN" (11px, uppercase, 0.08em letter-spacing, #6B7280) + `Send` icon (14px)
   - Right: `SecondLetterStatusBadge` (inline function or separate component)

2. **SecondLetterStatusBadge logic:**
   - Read `c.second_letter_status` (may be undefined or 'IDLE')
   - If `IDLE` or undefined: calculate countdown from creditors. Find `MAX(email_sent_at || document_sent_at)` across `c.final_creditor_list`. If found, compute `daysLeft = max(0, 30 - daysSince)`. Show pill badge: `"In {daysLeft} Tagen fällig"` in amber (#F59E0B) outlined+tinted style. If no email_sent_at found on any creditor, show nothing (1. Anschreiben not sent yet).
   - If `PENDING`: "Wartet auf Formular" — amber #F59E0B outlined pill
   - If `FORM_SUBMITTED`: "Formular eingereicht" — blue #3B82F6 outlined pill
   - If `SENT`: "Gesendet" — green #22C55E outlined pill
   - Badge style: `display: inline-flex, alignItems: center, gap: 5px, padding: 2px 10px, borderRadius: 9999px, border: 1px solid {color}40, background: {color}10, color: {color}, fontSize: 12px, fontWeight: 600`

3. **Action area (below header, 16px margin-top):**

   a. **Trigger button ("2. Anschreiben starten"):**
      - Always rendered (NEVER hidden per UI-01 requirement)
      - `disabled` when status is NOT IDLE (or undefined) — opacity 0.4, cursor not-allowed
      - Also disabled while mutation is loading (`isTriggeringSecondLetter`)
      - Style: orange accent (`#FF5001` bg, white text, 8px radius, 600 weight, 14px font, `8px 16px` padding), hover: `#E04500`
      - On click: open ConfirmActionDialog (NOT direct API call)
      - After mutation resolves: check `result.data.alreadyTriggered` — if true, show `toast.info('Client ist bereits im Status PENDING')`. If `result.data.success`, show `toast.success('2. Anschreiben erfolgreich gestartet')`. On error, show `toast.error(...)`.

   b. **ConfirmActionDialog for trigger:**
      - `title`: "2. Anschreiben starten?"
      - `description`: "Der Mandant wird per E-Mail benachrichtigt und aufgefordert, seine Finanzdaten zu bestätigen. Dieser Vorgang kann nicht rückgängig gemacht werden."
      - `confirmLabel`: "Starten"
      - `onConfirm`: calls `triggerSecondLetter(clientId)` via the RTK Query mutation hook

   c. **Plan type override (only when status === 'FORM_SUBMITTED' and snapshot exists):**
      - Render a row with label "Plan-Typ" (11px label) and a native `<select>` element (or the existing shadcn Select from `ui/select.tsx` if simpler).
      - Value: `c.second_letter_financial_snapshot?.plan_type || 'RATENPLAN'`
      - Options: "Ratenplan" (value RATENPLAN), "Nullplan" (value NULLPLAN)
      - `onValueChange`: call `overrideSecondLetterPlanType({ clientId, planType })` — show toast on success/error
      - Style: consistent with existing form inputs (8px radius, #E5E7EB border)

   d. **Send button ("Jetzt senden") — only when status === 'FORM_SUBMITTED':**
      - Green success button (#22C55E bg, white text), disabled while sending
      - On click: open a second ConfirmActionDialog
      - `title`: "2. Anschreiben jetzt senden?"
      - `description`: "Alle generierten DOCX-Dokumente werden per E-Mail an die Gläubiger gesendet. Dieser Vorgang kann nicht rückgängig gemacht werden."
      - `confirmLabel`: "Senden"
      - `onConfirm`: calls `sendSecondLetter(clientId)` — handles 207 (partial) with warning toast, 200 with success toast, errors with error toast

   e. **Sent summary (only when status === 'SENT'):**
      - Show `second_letter_sent_at` formatted as German date: "Gesendet am DD.MM.YYYY"
      - Use JetBrains Mono for the date

**State management:**
- `const [triggerDialogOpen, setTriggerDialogOpen] = useState(false);`
- `const [sendDialogOpen, setSendDialogOpen] = useState(false);`
- `const [triggerSecondLetter, { isLoading: isTriggeringSecondLetter }] = useTriggerSecondLetterMutation();`
- `const [sendSecondLetter, { isLoading: isSendingSecondLetter }] = useSendSecondLetterMutation();`
- `const [overridePlanType] = useOverrideSecondLetterPlanTypeMutation();`

These should be declared inside the `ClientDetail` component (the main component in client-detail.tsx) alongside the existing mutation hooks.

**Placement in renderOverview():**
Insert `<SecondLetterSection />` (or the JSX inline) between the `</QuickActions>` closing and `<PhasePrerequisites` opening. Add a `marginBottom: '16px'` to the section card for spacing.

**Design rules to follow:**
- No `#FFFFFF` on page background (card is #FFFFFF on #FAFAFA page — correct)
- No shadows, no gradients
- Badge: outlined + tinted, pill radius 9999px
- Font: DM Sans for text, JetBrains Mono for dates
- Max 1 orange CTA — the trigger button is the only orange element in this section
- Touch targets >= 44px height on buttons
  </action>
  <verify>
1. `cd MandantenPortalDesign && npx tsc --noEmit 2>&1 | head -30` — no TypeScript errors
2. Visually verify: The 2. Anschreiben section appears in the overview tab between QuickActions and PhasePrerequisites
3. Grep check: `grep -c 'SecondLetter' MandantenPortalDesign/src/app/components/client-detail.tsx` returns > 0
4. Grep check: `grep -c 'ConfirmActionDialog' MandantenPortalDesign/src/app/components/client-detail.tsx` — confirms dialog usage
  </verify>
  <done>
Client Detail overview shows a "2. Anschreiben" card section with:
- Status badge showing countdown/status text matching the four states
- Trigger button always visible, disabled when not IDLE, with confirmation modal
- Plan type override select visible only when FORM_SUBMITTED
- Send button visible only when FORM_SUBMITTED with confirmation modal
- Sent timestamp displayed when SENT
- AlreadyTriggered response handled with info toast (not error)
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `cd MandantenPortalDesign && npx tsc --noEmit` passes
2. Trigger button: rendered regardless of status, disabled when not IDLE
3. Confirmation modals: both trigger and send use ConfirmActionDialog
4. Status badge: renders correct text and color for all four states
5. Plan type select: only rendered when FORM_SUBMITTED
6. Send button: only rendered when FORM_SUBMITTED
</verification>

<success_criteria>
- 2. Anschreiben section visible in Client Detail overview tab
- Trigger button disabled (not hidden) when status is not IDLE
- ConfirmActionDialog prevents accidental triggers
- Status badge matches spec for all four states including IDLE countdown
- Plan type override persists via PATCH endpoint
- Send button dispatches emails and handles partial failure (207) gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/34-admin-ui-tracking/34-02-SUMMARY.md`
</output>
