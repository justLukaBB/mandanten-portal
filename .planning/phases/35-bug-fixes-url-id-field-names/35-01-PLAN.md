---
phase: 35-bug-fixes-url-id-field-names
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/secondLetterTriggerService.js
  - server/services/secondLetterDocumentGenerator.js
  - server/services/secondLetterCalculationService.js
autonomous: true
requirements:
  - NOTIF-02
  - SEND-02
  - DOC-03
  - DOC-04

must_haves:
  truths:
    - "Email deep-link opens the correct portal form page at /portal/second-letter-form"
    - "Per-creditor DOCX generation stores correct creditor_id (not undefined/empty) in result objects"
    - "MongoDB positional updates use 'final_creditor_list.id' to match creditor subdocuments that have { _id: false }"
    - "Template data for Familienstand and Unterhaltsberechtigte resolves from English snapshot field names (marital_status, number_of_dependents)"
    - "creditor_calculations entries store the actual creditor.id value (not empty string)"
    - "Per-creditor tracking in secondLetterService.js already uses correct 'final_creditor_list.id' filter (SEND-02 verified)"
  artifacts:
    - path: "server/services/secondLetterTriggerService.js"
      provides: "Correct portal deep-link URL"
      contains: "/portal/second-letter-form?token="
    - path: "server/services/secondLetterDocumentGenerator.js"
      provides: "Fixed _id→id references and field name fallbacks"
      contains: "creditor.id"
    - path: "server/services/secondLetterCalculationService.js"
      provides: "Fixed creditor_id storage in creditor_calculations"
      contains: "creditor.id ||"
  key_links:
    - from: "server/services/secondLetterTriggerService.js"
      to: "src/App.tsx route /portal/second-letter-form"
      via: "URL string in email body"
      pattern: "/portal/second-letter-form\\?token="
    - from: "server/services/secondLetterDocumentGenerator.js"
      to: "server/models/Client.js creditorSchema { _id: false }"
      via: "Mongoose positional update filter"
      pattern: "final_creditor_list\\.id"
    - from: "server/services/secondLetterCalculationService.js"
      to: "server/services/secondLetterDocumentGenerator.js prepareTemplateData"
      via: "creditor_id field in creditor_calculations array"
      pattern: "creditor\\.id"
---

<objective>
Fix all data-level bugs identified by the v10 milestone audit: incorrect email deep-link URL, `_id` vs `id` mismatches in MongoDB positional updates and creditor ID storage, and German/English field name mismatch in template data preparation.

Purpose: These bugs prevent the 2. Anschreiben workflow from functioning end-to-end. The email link sends clients to a 404, DOCX generation fails to persist filenames (positional updates match nothing), and template variables for Familienstand/Unterhaltsberechtigte render as empty.

Output: Three backend service files corrected with all six bug sites fixed, plus SEND-02 verified as already correct.
</objective>

<execution_context>
@/Users/luka.s/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luka.s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/35-bug-fixes-url-id-field-names/35-RESEARCH.md

# Key reference files (read to confirm schema and route)
@server/models/Client.js (lines 85-219: creditorSchema with { _id: false } and id: String)
@server/models/Client.js (lines 655-678: second_letter_financial_snapshot with marital_status, number_of_dependents)
@src/App.tsx (line 118: SecondLetterForm mounted at /portal/second-letter-form)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix _id-to-id and URL bugs in secondLetterDocumentGenerator and secondLetterTriggerService</name>
  <files>
    server/services/secondLetterTriggerService.js
    server/services/secondLetterDocumentGenerator.js
  </files>
  <action>
**Bug 1 — NOTIF-02: Fix email deep-link URL** in `server/services/secondLetterTriggerService.js`:
- Line 93: Change `/second-letter?token=` to `/portal/second-letter-form?token=`
- This matches the route in `src/App.tsx` line 118 where SecondLetterForm is mounted.

**Bug 2 — DOC-03: Fix field name fallbacks in prepareTemplateData** in `server/services/secondLetterDocumentGenerator.js`:
- Line 235: Change `snapshot.familienstand || ''` to `snapshot.familienstand || snapshot.marital_status || ''`
- Line 236: Change `String(snapshot.anzahl_unterhaltsberechtigte || 0)` to `String(snapshot.anzahl_unterhaltsberechtigte ?? snapshot.number_of_dependents ?? 0)`
- Use `??` (not `||`) for `number_of_dependents` because `0` is a valid value that `||` would skip.
- This matches the proven fallback pattern in `secondLetterCalculationService.js` lines 61-65.

**Bug 3 — DOC-04a: Fix creditor_id in generateForSingleCreditor result** in `server/services/secondLetterDocumentGenerator.js`:
- Line 300: Change `creditor._id?.toString()` to `creditor.id`
- Line 341 (error object in loop): Change `creditor._id?.toString()` to `creditor.id`
- Reason: `creditorSchema` has `{ _id: false }`, so `creditor._id` is always `undefined`.

**Bug 4 — DOC-04b: Fix DB update filter in generateForAllCreditors** in `server/services/secondLetterDocumentGenerator.js`:
- Line 358: Change `'final_creditor_list._id': result.creditor_id` to `'final_creditor_list.id': result.creditor_id`
- Combined with Bug 3 fix, `result.creditor_id` now contains `creditor.id` (a real value), and the filter key matches the schema field.

**Bug 5 — DOC-04c: Fix creditor lookup and DB update in generateForSingleCreditorById** in `server/services/secondLetterDocumentGenerator.js`:
- Line 398: Change `c._id.toString() === creditorId` to `c.id === creditorId`
- Line 410: Change `'final_creditor_list._id': creditor._id` to `'final_creditor_list.id': creditor.id`
- Reason: Same `{ _id: false }` issue. `c._id.toString()` would throw TypeError since `_id` is undefined.

Do NOT change line 203 (`creditor._id?.toString() || creditor.id`) — the fallback to `creditor.id` already works correctly once Bug 6 (Task 2) is fixed.
  </action>
  <verify>
Run `grep -n '_id' server/services/secondLetterDocumentGenerator.js` and confirm:
- NO occurrences of `creditor._id` remain (except line 319 which is `client._id` — that is correct, Client documents DO have `_id`)
- NO occurrences of `'final_creditor_list._id'` remain
- Line 93 of triggerService contains `/portal/second-letter-form?token=`
  </verify>
  <done>
- secondLetterTriggerService.js line 93 has URL `/portal/second-letter-form?token=`
- secondLetterDocumentGenerator.js uses `creditor.id` (not `creditor._id`) in all creditor identifier references
- secondLetterDocumentGenerator.js uses `'final_creditor_list.id'` (not `'final_creditor_list._id'`) in all Mongoose positional update filters
- prepareTemplateData has fallback chain: `snapshot.familienstand || snapshot.marital_status` and `snapshot.anzahl_unterhaltsberechtigte ?? snapshot.number_of_dependents`
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix creditor_id storage in calculationService and verify SEND-02</name>
  <files>
    server/services/secondLetterCalculationService.js
    server/services/secondLetterService.js
  </files>
  <action>
**Bug 6 — Fix creditor_id in calculateSecondLetterFinancials** in `server/services/secondLetterCalculationService.js`:
- Line 145: Change `creditor._id?.toString() || ''` to `creditor.id || ''`
- Reason: `creditorSchema` has `{ _id: false }`, so `creditor._id` is always `undefined`, causing `?.toString()` to return `undefined`, which `|| ''` converts to empty string. This means every `creditor_calculations` entry has `creditor_id: ''`, breaking the per-creditor lookup in `prepareTemplateData`.
- After this fix, new snapshots will store the real `creditor.id` value (e.g., `"abc-123"`) in `creditor_calculations[].creditor_id`.

**SEND-02 verification** — Read `server/services/secondLetterService.js` line 147:
- Current code: `{ _id: client._id, 'final_creditor_list.id': creditor.id }` — this is ALREADY CORRECT.
- The `final_creditor_list.id` filter key matches the creditorSchema field, and `creditor.id` is the correct identifier.
- No code change needed. Document SEND-02 as satisfied.

**IMPORTANT operational note for plan output:** After deploying these fixes, any client with `second_letter_status: FORM_SUBMITTED` that had their snapshot calculated BEFORE this fix will have `creditor_id: ''` in all `creditor_calculations` entries. The admin must run the recalculate endpoint (`POST /api/admin/clients/:clientId/recalculate-second-letter`) for those clients before DOCX generation will produce correct financial figures.
  </action>
  <verify>
Run `grep -n 'creditor._id' server/services/secondLetterCalculationService.js` — should return NO results.
Run `grep -n 'creditor.id' server/services/secondLetterCalculationService.js` — line 145 should show `creditor.id || ''`.
Run `grep -n 'final_creditor_list.id' server/services/secondLetterService.js` — should show line 147 with correct filter key (no change needed).
  </verify>
  <done>
- secondLetterCalculationService.js line 145 stores `creditor.id` (not `creditor._id?.toString()`) in `creditor_calculations[].creditor_id`
- secondLetterService.js line 147 confirmed to already use `'final_creditor_list.id': creditor.id` (SEND-02 satisfied, no change needed)
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify all six bug sites are resolved:

1. **NOTIF-02 (URL):** `grep '/portal/second-letter-form' server/services/secondLetterTriggerService.js` returns line 93
2. **DOC-03 (field names):** `grep 'marital_status' server/services/secondLetterDocumentGenerator.js` returns line 235
3. **DOC-03 (field names):** `grep 'number_of_dependents' server/services/secondLetterDocumentGenerator.js` returns line 236
4. **DOC-04 (creditor.id):** `grep -c 'creditor\._id' server/services/secondLetterDocumentGenerator.js` returns 0
5. **DOC-04 (filter key):** `grep -c 'final_creditor_list\._id' server/services/secondLetterDocumentGenerator.js` returns 0
6. **DOC-04 (calc service):** `grep -c 'creditor\._id' server/services/secondLetterCalculationService.js` returns 0
7. **SEND-02 (already correct):** `grep 'final_creditor_list.id' server/services/secondLetterService.js` shows correct filter

Run `node -e "require('./server/services/secondLetterDocumentGenerator')"` — no require-time errors.
Run `node -e "require('./server/services/secondLetterCalculationService')"` — no require-time errors.
Run `node -e "require('./server/services/secondLetterTriggerService')"` — may fail due to DI, acceptable.
</verification>

<success_criteria>
- All four requirements (NOTIF-02, SEND-02, DOC-03, DOC-04) are addressed
- Zero occurrences of `creditor._id` in secondLetterDocumentGenerator.js and secondLetterCalculationService.js
- Zero occurrences of `'final_creditor_list._id'` in secondLetterDocumentGenerator.js
- Email deep-link URL matches the route in src/App.tsx
- Template data fallback chain handles both German and English snapshot field names
- No new files created, no new dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/35-bug-fixes-url-id-field-names/35-01-SUMMARY.md`
</output>
