---
phase: 38-fix-schema-gap-persist-calculation-fields
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/models/Client.js
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - CALC-04
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - SEND-01
  - SEND-03
  - SEND-04

must_haves:
  truths:
    - "Calculation results (creditor_calculations, calculation_status, calculated_at, total_debt, calculation_error) persist to MongoDB after $set write via findByIdAndUpdate"
    - "The send-second-letter route guard passes when calculation_status is 'completed' — DOCX generation and email dispatch proceed"
    - "REQUIREMENTS.md shows all 8 requirements (CALC-04, DOC-01–04, SEND-01, SEND-03, SEND-04) as checked and Complete in traceability"
  artifacts:
    - path: "server/models/Client.js"
      provides: "5 missing fields declared in second_letter_financial_snapshot subdocument"
      contains: "creditor_calculations"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated checkboxes and traceability for 8 requirements"
      contains: "CALC-04"
  key_links:
    - from: "server/models/Client.js"
      to: "server/controllers/clientPortalController.js"
      via: "Mongoose schema whitelists $set fields at line ~1479"
      pattern: "second_letter_financial_snapshot\\.creditor_calculations"
    - from: "server/models/Client.js"
      to: "server/routes/admin-second-letter.js"
      via: "Mongoose schema whitelists $set fields at line ~78; route guard at line ~121 reads calculation_status"
      pattern: "snapshot\\.calculation_status"
---

<objective>
Add 5 missing field declarations to the `second_letter_financial_snapshot` Mongoose subdocument schema so calculation results persist to MongoDB — unblocking the entire DOCX generation + email dispatch + status transition pipeline.

Purpose: Mongoose strict mode (default since v5) silently discards undeclared fields in $set operations. Phase 31's calculation service writes 5 fields that are not declared in the schema. This single schema edit unblocks 8 requirements across 4 phases (31, 32, 33, 36).

Output: Working schema declaration + updated requirements documentation
</objective>

<execution_context>
@/Users/luka.s/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luka.s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-fix-schema-gap-persist-calculation-fields/38-RESEARCH.md
@server/models/Client.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 missing field declarations to second_letter_financial_snapshot schema</name>
  <files>server/models/Client.js</files>
  <action>
In `server/models/Client.js`, locate the `second_letter_financial_snapshot` subdocument definition (currently lines ~655–678). After the `snapshot_created_at: Date` field (the current last field before the closing `}`), add these 5 field declarations:

```javascript
    snapshot_created_at: Date,
    // Phase 38: calculation result fields — declared so Mongoose strict mode persists them
    creditor_calculations: [{
      creditor_id: String,
      creditor_name: String,
      claim_amount: Number,
      tilgungsangebot: Number,
      quota_percentage: Number
    }],
    calculation_status: {
      type: String,
      enum: ['completed', 'failed']
    },
    calculation_error: String,
    calculated_at: Date,
    total_debt: Number
```

Critical details:
- `creditor_calculations` MUST be a typed array of subdocuments (not `Array` or `[{}]`) — field names must match exactly what Phase 31's secondLetterCalculationService writes: `creditor_id`, `creditor_name`, `claim_amount`, `tilgungsangebot`, `quota_percentage`
- `calculation_status` enum MUST be `['completed', 'failed']` with NO default — `undefined` is the correct sentinel for "not yet calculated"
- `calculation_error` is plain `String` with no `required` constraint — null writes from success path must be valid
- Do NOT change any existing fields. Do NOT touch any write-side code (clientPortalController.js, admin-second-letter.js). The writes are already correct.
- Do NOT add `_id: false` to the creditor_calculations subdocument (Mongoose adds _id by default to subdocument arrays, which is fine here)
  </action>
  <verify>
1. `grep -n "creditor_calculations" server/models/Client.js` — should show the field inside second_letter_financial_snapshot
2. `grep -n "calculation_status" server/models/Client.js` — should show enum declaration
3. `grep -n "total_debt" server/models/Client.js` — should show Number type
4. `node -e "const m = require('./server/models/Client.js'); console.log('Schema loaded OK')"` — should not throw (validates schema syntax)
  </verify>
  <done>
5 fields (creditor_calculations, calculation_status, calculation_error, calculated_at, total_debt) are declared inside the second_letter_financial_snapshot subdocument in Client.js. The schema loads without errors. No existing fields were changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md checkboxes and traceability for 8 unblocked requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
In `.planning/REQUIREMENTS.md`, update all 8 requirements that Phase 38 unblocks:

**Checkboxes to flip (from `- [ ]` to `- [x]`):**
1. `CALC-04` — Tilgungsangebot pro Glaebiger berechnet und im Snapshot gespeichert
2. `DOC-01` — SecondLetterDocumentGenerator erstellt
3. `DOC-02` — Template-Branching: plan_type == RATENPLAN / NULLPLAN Template
4. `DOC-03` — Template-Variablen befuellt
5. `DOC-04` — Ein DOCX pro Glaebiger generiert, gespeichert in generated_documents/second_round/
6. `SEND-01` — Resend Email pro Glaebiger mit DOCX Attachment
7. `SEND-03` — Zendesk Audit-Comment pro erfolgreichem Versand
8. `SEND-04` — Status-Uebergang FORM_SUBMITTED -> SENT nach erfolgreichem Versand

**Traceability table updates (change Status from `Pending` to `Complete`):**
- CALC-04: Status → Complete
- DOC-01: Status → Complete
- DOC-02: Status → Complete
- DOC-03: Status → Complete
- DOC-04: Status → Complete
- SEND-01: Status → Complete
- SEND-03: Status → Complete
- SEND-04: Status → Complete

**Update Last updated line** at the bottom of the file to reflect today's date and Phase 38 completion.

Do NOT change any other checkboxes or traceability rows. Only the 8 listed above.
  </action>
  <verify>
1. `grep -c "\- \[x\]" .planning/REQUIREMENTS.md` — count should be 34 (all v10 requirements complete)
2. `grep -c "Pending" .planning/REQUIREMENTS.md` — count should be 0
3. `grep "CALC-04" .planning/REQUIREMENTS.md` — should show `[x]` and `Complete`
4. `grep "DOC-01" .planning/REQUIREMENTS.md` — should show `[x]` and `Complete`
5. `grep "SEND-04" .planning/REQUIREMENTS.md` — should show `[x]` and `Complete`
  </verify>
  <done>
All 8 requirement checkboxes are checked. All 8 traceability rows show Complete. No other requirements were modified. The coverage section shows 34/34 requirements mapped and complete.
  </done>
</task>

</tasks>

<verification>
1. Schema syntax: `node -e "require('./server/models/Client.js')"` loads without error
2. Field presence: `grep -A5 "creditor_calculations" server/models/Client.js` shows typed array subdocument
3. Requirements coverage: `grep -c "Pending" .planning/REQUIREMENTS.md` returns 0
4. No unintended changes: `git diff --stat` shows only Client.js and REQUIREMENTS.md modified
</verification>

<success_criteria>
1. The 5 fields (creditor_calculations, calculation_status, calculation_error, calculated_at, total_debt) are declared in the second_letter_financial_snapshot subdocument schema
2. Mongoose schema loads without syntax errors
3. All 8 REQUIREMENTS.md checkboxes (CALC-04, DOC-01–04, SEND-01, SEND-03, SEND-04) are marked [x] Complete
4. No write-side code was changed — only schema declaration and documentation
</success_criteria>

<output>
After completion, create `.planning/phases/38-fix-schema-gap-persist-calculation-fields/38-01-SUMMARY.md`
</output>
