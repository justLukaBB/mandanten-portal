---
phase: 27-polish-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MandantenPortalDesign/package.json
  - MandantenPortalDesign/src/utils/exportQueue.ts
  - MandantenPortalDesign/src/app/components/review-queue-page.tsx
autonomous: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "Admin clicks Export button and a CSV file downloads containing all review queue data"
    - "Admin selects XLSX from dropdown and an XLSX file downloads containing all review queue data"
    - "Export includes all visible columns (Name, Aktenzeichen, Gläubiger, Priorität, Confidence, Zugewiesen) regardless of current page/filter"
  artifacts:
    - path: "MandantenPortalDesign/src/utils/exportQueue.ts"
      provides: "CSV and XLSX export utility functions"
      exports: ["exportQueueAsCSV", "exportQueueAsXLSX"]
    - path: "MandantenPortalDesign/src/app/components/review-queue-page.tsx"
      provides: "Export button in queue header"
      contains: "exportQueue"
  key_links:
    - from: "MandantenPortalDesign/src/app/components/review-queue-page.tsx"
      to: "MandantenPortalDesign/src/utils/exportQueue.ts"
      via: "import and button onClick"
      pattern: "exportQueueAs(CSV|XLSX)"
    - from: "MandantenPortalDesign/src/utils/exportQueue.ts"
      to: "xlsx"
      via: "library import"
      pattern: "import.*xlsx"
---

<objective>
Add CSV/XLSX export functionality to the Review Queue page.

Purpose: Admins need to export review queue data for reporting, offline analysis, and audit trail purposes.
Output: Export button in the queue header with CSV default and XLSX dropdown option, downloading the full queue regardless of active filters.
</objective>

<execution_context>
@/Users/luka.s/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luka.s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-queue-management/25-02-SUMMARY.md
@MandantenPortalDesign/src/app/components/review-queue-page.tsx
@MandantenPortalDesign/src/store/api/reviewApi.ts
@MandantenPortalDesign/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install xlsx library and create export utility</name>
  <files>
    MandantenPortalDesign/package.json
    MandantenPortalDesign/src/utils/exportQueue.ts
  </files>
  <action>
Install the xlsx library in MandantenPortalDesign:
```
cd MandantenPortalDesign && npm install xlsx
```

Create `MandantenPortalDesign/src/utils/exportQueue.ts` with two exported functions:

1. `exportQueueAsCSV(clients: AdminReviewQueueClient[])` — Takes the full queue data array and generates a CSV file download. Use xlsx library's `XLSX.utils.json_to_sheet()` then `XLSX.utils.sheet_to_csv()` to generate CSV string, then create a Blob and trigger download via anchor click with `URL.createObjectURL`. Filename: `review-queue-{YYYY-MM-DD}.csv`.

2. `exportQueueAsXLSX(clients: AdminReviewQueueClient[])` — Same data but uses `XLSX.writeFile()` to generate and download an .xlsx file. Filename: `review-queue-{YYYY-MM-DD}.xlsx`.

Both functions should map the AdminReviewQueueClient fields to German column headers matching the queue table:
- `name` → "Name"
- `aktenzeichen` → "Aktenzeichen"
- `creditor_count` → "Gläubiger"
- `review_creditor_count` → "Review Gläubiger"
- `priority` → "Priorität" (capitalize: Hoch/Mittel/Niedrig)
- `priority_score` → "Prioritäts-Score"
- `avg_confidence` → "Confidence (%)"
- `days_since_payment` → "Tage seit Zahlung"
- `review_assignment.assigned_to` → "Zugewiesen an" (empty string if null)

Import the AdminReviewQueueClient type from `../../store/api/reviewApi`.

Use the `xlsx` library's SheetAOA or json_to_sheet approach — both are fine. The key is that the exported file has German headers, not raw field names.
  </action>
  <verify>
    - `cd MandantenPortalDesign && npx tsc --noEmit` passes without errors
    - `node -e "require('xlsx')"` in MandantenPortalDesign directory does not throw
    - `src/utils/exportQueue.ts` exists and exports both functions
  </verify>
  <done>xlsx installed, exportQueue utility exports both CSV and XLSX download functions with German column headers</done>
</task>

<task type="auto">
  <name>Task 2: Add export button with dropdown to ReviewQueuePage</name>
  <files>
    MandantenPortalDesign/src/app/components/review-queue-page.tsx
  </files>
  <action>
In `review-queue-page.tsx`, add an export button in the queue header area (top-right, near the existing search/filter controls).

**Import:** Add imports for `exportQueueAsCSV` and `exportQueueAsXLSX` from `../../utils/exportQueue`, and the `Download` icon from `lucide-react`.

**Fetch full queue for export:** The export must include ALL queue items regardless of current filters/pagination. When the admin clicks export, use a separate RTK Query lazy query or a direct fetch call to `GET /api/admin/review/queue?limit=9999` (large limit to get all items). This avoids exporting only the current page.

Implementation approach: Use `useLazyGetAdminReviewQueueQuery` from reviewApi. When the export button is clicked:
1. Trigger the lazy query with `{ limit: 9999 }` (no search/priority filter — full queue per CONTEXT.md decision)
2. On success, call the appropriate export function with the returned clients array
3. Show a loading spinner on the button during fetch

**Button UI:** Use a split-button or DropdownMenu approach (Claude's discretion per CONTEXT.md):
- Primary button: "CSV Export" with Download icon — triggers CSV download
- DropdownMenu item: "XLSX Export" — triggers XLSX download

Style the button consistent with the existing queue page design:
- White background, 1px #E5E7EB border, 12px radius (matching design guidelines)
- Text: 14px, DM Sans, #374151
- Hover: light gray background

Place the button in the header row where the search bar and filter controls already live, aligned to the right side.

During export loading, show a small spinner inside the button (same CSS-only spinner pattern used in EditableCell: border-t-transparent animate-spin).
  </action>
  <verify>
    - `cd MandantenPortalDesign && npx tsc --noEmit` passes
    - Visually: Export button visible in queue header when dev server is running
    - Clicking CSV Export triggers a file download
    - Clicking XLSX Export triggers an .xlsx file download
  </verify>
  <done>Export button with CSV default and XLSX dropdown option is visible in queue header; clicking either option fetches full queue and triggers browser file download</done>
</task>

</tasks>

<verification>
1. `cd MandantenPortalDesign && npx tsc --noEmit` — no TypeScript errors
2. `npm run dev` in MandantenPortalDesign — app loads without console errors
3. Navigate to /review — Export button visible in header area
4. Click CSV Export — browser downloads `review-queue-{date}.csv` with German headers and all queue data
5. Click XLSX Export — browser downloads `review-queue-{date}.xlsx` with German headers and all queue data
</verification>

<success_criteria>
- Admin can export the review queue as CSV or XLSX file
- Export contains all queue items (not just current page)
- Column headers are in German matching the table display
- Export button is visually consistent with the queue page design
</success_criteria>

<output>
After completion, create `.planning/phases/27-polish-migration/27-01-SUMMARY.md`
</output>
