---
phase: 17-schema-webhook-field-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/models/Client.js
  - server/controllers/webhookController.js
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - HOOK-01
  - HOOK-02

must_haves:
  truths:
    - "A FastAPI payload with all 5 new fields results in them persisted on the creditor in MongoDB"
    - "A FastAPI payload with all 5 new fields results in them stored on the document's extracted_data.creditor_data"
    - "A FastAPI payload omitting any of the 5 new fields does not cause a validation error"
    - "When enrichDedupedCreditorFromDb overwrites an address from local DB, address_source is set to local_db"
  artifacts:
    - path: "server/models/Client.js"
      provides: "creditorSchema + documentSchema with 5 new fields"
      contains: "aktenzeichen_glaeubigervertreter"
    - path: "server/controllers/webhookController.js"
      provides: "address_source = local_db enrichment logic"
      contains: "address_source"
  key_links:
    - from: "server/controllers/webhookController.js"
      to: "server/models/Client.js"
      via: "creditorSchema fields flow from FastAPI payload through enrichment to MongoDB save"
      pattern: "address_source.*local_db"
---

<objective>
Add 5 new FastAPI fields (aktenzeichen_glaeubigervertreter, address_source, llm_address_original, glaeubiger_adresse_ist_postfach, glaeubiger_vertreter_adresse_ist_postfach) to Mongoose schemas and wire address_source enrichment logic.

Purpose: FastAPI's accuracy milestone extracts these fields. Node.js must accept and persist them so downstream merge logic (Phase 18) and future UI can consume them.

Output: Updated Client.js schemas + webhookController enrichment logic
</objective>

<execution_context>
@/Users/luka.s/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luka.s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/models/Client.js
@server/controllers/webhookController.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 new fields to creditorSchema and documentSchema</name>
  <files>server/models/Client.js</files>
  <action>
Add the following 5 fields to TWO locations in server/models/Client.js:

**Location 1 — creditorSchema (around line 80-184):**
Add after the `glaeubigervertreter_adresse` field (line 96):
- `aktenzeichen_glaeubigervertreter: String` — creditor representative reference number
- `address_source: String` — tracks where the address came from (e.g. "llm", "local_db", "postfach")
- `llm_address_original: String` — preserves the original LLM-extracted address before DB overwrite
- `glaeubiger_adresse_ist_postfach: { type: Boolean, default: false }` — creditor address is a PO box
- `glaeubiger_vertreter_adresse_ist_postfach: { type: Boolean, default: false }` — representative address is a PO box

**Location 2 — documentSchema.extracted_data.creditor_data (around line 36-44):**
Add after the `actual_creditor` field (line 43):
- `aktenzeichen_glaeubigervertreter: String`
- `address_source: String`
- `llm_address_original: String`
- `glaeubiger_adresse_ist_postfach: { type: Boolean, default: false }`
- `glaeubiger_vertreter_adresse_ist_postfach: { type: Boolean, default: false }`

String fields default to null (Mongoose default for unset String). Boolean fields default to false.
This ensures payloads omitting these fields cause no validation errors (SCHEMA-01, SCHEMA-02).

Note: The webhook controller already passes `extracted_data` wholesale to documents and spreads creditor objects into final_creditor_list. No additional mapping code is needed for HOOK-01 — Mongoose schema acceptance is sufficient for the fields to flow through and persist.
  </action>
  <verify>
Run `node -e "const m = require('./server/models/Client'); const s = m.schema.path('final_creditor_list.aktenzeichen_glaeubigervertreter'); console.log('creditor field exists:', !!s); const d = m.schema.path('documents.extracted_data.creditor_data.aktenzeichen_glaeubigervertreter'); console.log('doc field exists:', !!d);"` from the project root — both should print true.
  </verify>
  <done>creditorSchema and documentSchema both contain all 5 new fields with correct types and defaults. Payloads with or without these fields are accepted without validation errors.</done>
</task>

<task type="auto">
  <name>Task 2: Set address_source to local_db when enrichment overwrites address</name>
  <files>server/controllers/webhookController.js</files>
  <action>
Modify TWO enrichment functions in webhookController.js to set `address_source = "local_db"` when they replace an address from the local creditor database:

**Function 1 — enrichCreditorContactFromDb (around line 69-165):**
This function enriches document-level creditor_data. When it overwrites `address` or `sender_address` from a DB match:
- After `updatedCreditorData.address = match.address;` (line 130) or `updatedCreditorData.sender_address = match.address;` (line 134), if EITHER address was actually replaced (was missing, now filled from DB), set:
  ```js
  updatedCreditorData.address_source = 'local_db';
  ```
- Add this AFTER both address assignment blocks, with a condition:
  ```js
  if ((missingAddress && match.address) || (missingSenderAddress && match.address)) {
      updatedCreditorData.address_source = 'local_db';
  }
  ```

**Function 2 — enrichDedupedCreditorFromDb (around line 172-238):**
This function enriches creditor entries in final_creditor_list. When it replaces the creditor's address from a DB match:
- In the creditor section (around line 211-215), after setting `entry.glaeubiger_adresse = match.address` and `entry.sender_address = match.address`, add:
  ```js
  entry.address_source = 'local_db';
  ```
- Wrap in the same condition that already gates the address replacement: `if (needAddr && match.address)` — so the line goes INSIDE that block, after the two address assignments.

Do NOT set address_source when only email is enriched (email enrichment does not change the address provenance).
Do NOT overwrite address_source if the address was NOT actually replaced (i.e., address was already present).
  </action>
  <verify>
Grep for `address_source.*local_db` in webhookController.js — should find exactly 2 occurrences (one in each enrichment function). Run `node -c server/controllers/webhookController.js` to confirm no syntax errors.
  </verify>
  <done>Both enrichCreditorContactFromDb and enrichDedupedCreditorFromDb set address_source to "local_db" when they overwrite an address from the local database. address_source is untouched when no address replacement occurs.</done>
</task>

</tasks>

<verification>
1. `node -c server/models/Client.js` — no syntax errors
2. `node -c server/controllers/webhookController.js` — no syntax errors
3. Schema validation: new fields exist with correct types
4. Grep confirms address_source = 'local_db' in both enrichment functions
</verification>

<success_criteria>
- All 5 new fields accepted by Mongoose in both creditorSchema and documentSchema.extracted_data.creditor_data
- Payloads omitting the fields do not cause errors (String defaults to null, Boolean defaults to false)
- enrichCreditorContactFromDb sets address_source = "local_db" on address replacement
- enrichDedupedCreditorFromDb sets address_source = "local_db" on address replacement
- No existing functionality is broken (existing fields, enrichment logic, merge flow)
</success_criteria>

<output>
After completion, create `.planning/phases/17-schema-webhook-field-mapping/17-01-SUMMARY.md`
</output>
