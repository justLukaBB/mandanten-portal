---
phase: 22-client-detail
plan: 03
type: execute
wave: 2
depends_on:
  - "22-01"
files_modified:
  - MandantenPortalDesign/src/app/components/client-detail.tsx
autonomous: true
requirements:
  - DETAIL-04

must_haves:
  truths:
    - "Admin opens the Gläubiger tab and sees all creditors from client.final_creditor_list — not mock data"
    - "The creditor table shows all existing columns (Manuelle Prüfung, Prüfungsgrund, Dokumenttyp, Gläubiger Name, Aktenzeichen, Gläubiger Adresse, Forderungsbetrag, Email Gläubiger) with real data"
    - "The table includes 5 additional v7 columns: Aktenzeichen Gläubigervertreter, Adressquelle, LLM Original-Adresse, Postfach (Gläubiger), Postfach (Vertreter)"
    - "The creditor count in the header matches the actual number of creditors"
    - "Empty creditor list shows 'Keine Gläubiger vorhanden' message"
    - "Creditors with needs_manual_review=true show 'Ja' badge in amber; others show 'Nein' in green"
  artifacts:
    - path: "MandantenPortalDesign/src/app/components/client-detail.tsx"
      provides: "Gläubiger tab rendering real creditor data including all v7 fields in a scrollable table"
      contains: "aktenzeichen_glaeubigervertreter"
  key_links:
    - from: "MandantenPortalDesign/src/app/components/client-detail.tsx renderCreditors"
      to: "client.final_creditor_list"
      via: "Iterates over client.final_creditor_list array from useGetClientDetailQuery"
      pattern: "client\\.final_creditor_list"
---

<objective>
Wire the Gläubiger tab in the ClientDetail component to display real creditor data from client.final_creditor_list, including all existing fields and the 5 new v7 fields (aktenzeichen_glaeubigervertreter, address_source, llm_address_original, glaeubiger_adresse_ist_postfach, glaeubiger_vertreter_adresse_ist_postfach).

Purpose: The creditor table is the most data-rich tab. Admins need to see all creditor details including the v7 fields that track address provenance and Postfach flags — critical for verifying that the AI deduplication and address enrichment pipeline is working correctly.

Output: A fully functional Gläubiger tab with all creditor fields including v7 fields rendered in a horizontally scrollable table.
</objective>

<execution_context>
@/Users/luka.s/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luka.s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-client-detail/22-01-SUMMARY.md

# The file we are modifying (refactored in Plan 22-01)
@MandantenPortalDesign/src/app/components/client-detail.tsx
@MandantenPortalDesign/src/app/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Gläubiger tab to real creditor data with all fields including v7</name>
  <files>
    MandantenPortalDesign/src/app/components/client-detail.tsx
  </files>
  <action>
**Refactor the `renderCreditors` function** in client-detail.tsx to use real `client.final_creditor_list` data with expanded columns for v7 fields.

1. **Creditor count header:** Change `GLÄUBIGER ({totalCreditors})` to use `client.final_creditor_list?.length ?? 0`.

2. **Empty state:** If `client.final_creditor_list` is empty or undefined, show:
   ```tsx
   <div style={{ padding: '40px 20px', textAlign: 'center', color: '#6B7280' }}>
     <div style={{ fontSize: '14px', fontWeight: 500 }}>Keine Gläubiger vorhanden</div>
     <div style={{ fontSize: '12px', marginTop: '4px' }}>Für diesen Mandanten wurden noch keine Gläubiger erfasst.</div>
   </div>
   ```

3. **Expand the table columns.** The prototype has 8 columns. Add 5 more for the v7 fields. New column layout (13 total):
   ```
   Manuelle Prüfung | Prüfungsgrund | Dokumenttyp | Gläubiger Name | Aktenzeichen | Gläubiger Adresse | Forderungsbetrag | Email Gläubiger | Akt.z. Gläubigervertreter | Adressquelle | LLM Original-Adresse | Postfach (Gl.) | Postfach (Vertr.)
   ```

   **Grid template columns** — Expand the gridTemplateColumns to accommodate 13 columns. Suggested widths:
   ```
   120px 140px 140px 200px 150px 250px 130px 180px 180px 120px 250px 100px 100px
   ```
   Total: ~1960px — the scroll container already handles horizontal overflow.
   Update the `minWidth` on both header and body rows to ~2000px.

4. **Map real creditor fields to columns:**
   For each creditor in `client.final_creditor_list`:
   - **Manuelle Prüfung:** `creditor.needs_manual_review` → "Ja" (amber badge) or "Nein" (green badge). Same badge style as prototype.
   - **Prüfungsgrund:** `creditor.review_reasons?.join(', ') || '–'`
   - **Dokumenttyp:** `creditor.dokumenttyp || '–'`
   - **Gläubiger Name:** `creditor.glaeubiger_name || creditor.sender_name || '–'` (German field preferred, fallback to English)
   - **Aktenzeichen:** `creditor.reference_number || '–'` (monospace, JetBrains Mono)
   - **Gläubiger Adresse:** `creditor.glaeubiger_adresse || creditor.sender_address || '–'` (truncated with text-overflow: ellipsis)
   - **Forderungsbetrag:** `creditor.forderungbetrag || creditor.claim_amount_raw || (creditor.claim_amount ? new Intl.NumberFormat('de-DE', { style: 'currency', currency: 'EUR' }).format(creditor.claim_amount) : '–')` (monospace, right-aligned)
   - **Email Gläubiger:** `creditor.email_glaeubiger || creditor.sender_email || '–'` (truncated)
   - **Akt.z. Gläubigervertreter** (v7): `creditor.aktenzeichen_glaeubigervertreter || '–'` (monospace)
   - **Adressquelle** (v7): `creditor.address_source || '–'`. If value is 'local_db', show it in a small blue badge (`border: 1px solid #3B82F6, background: #3B82F60D, color: #3B82F6`). Otherwise show raw value.
   - **LLM Original-Adresse** (v7): `creditor.llm_address_original || '–'` (truncated with ellipsis, tooltip on hover if possible — or just truncate)
   - **Postfach (Gl.)** (v7): `creditor.glaeubiger_adresse_ist_postfach` → "Ja" (amber text) or "Nein" (gray text). Simple text, no badge needed.
   - **Postfach (Vertr.)** (v7): `creditor.glaeubiger_vertreter_adresse_ist_postfach` → "Ja" (amber text) or "Nein" (gray text)

5. **Keep the action buttons** in the header (AI Re-Dedup, + Hinzufügen, Exportieren XLSX) but keep them as visual elements only — no backend wiring needed in this plan. They are design elements from the prototype.

6. **Preserve the prototype's visual design** for the existing 8 columns — same padding, font sizes, hover effects, border styles, badge colors. Extend the pattern to the 5 new v7 columns.

7. **Header row styling for v7 columns:** The 5 new columns should have the same header styling (12px, #6B7280, fontWeight 500) as the existing columns.
  </action>
  <verify>
- `grep "final_creditor_list" MandantenPortalDesign/src/app/components/client-detail.tsx` — present (uses real data)
- `grep "aktenzeichen_glaeubigervertreter" MandantenPortalDesign/src/app/components/client-detail.tsx` — present (v7 field)
- `grep "address_source" MandantenPortalDesign/src/app/components/client-detail.tsx` — present (v7 field)
- `grep "llm_address_original" MandantenPortalDesign/src/app/components/client-detail.tsx` — present (v7 field)
- `grep "glaeubiger_adresse_ist_postfach" MandantenPortalDesign/src/app/components/client-detail.tsx` — present (v7 field)
- `grep "glaeubiger_vertreter_adresse_ist_postfach" MandantenPortalDesign/src/app/components/client-detail.tsx` — present (v7 field)
- `grep "Keine Gläubiger" MandantenPortalDesign/src/app/components/client-detail.tsx` — present (empty state)
- The old hardcoded `creditorsTable` array should NOT appear
- `cd MandantenPortalDesign && npx tsc --noEmit` — no TypeScript errors
  </verify>
  <done>
Gläubiger tab displays all creditors from client.final_creditor_list with 13 columns including 5 v7 fields. Manual review badges show Ja/Nein. German field names preferred with English fallbacks. Address source shows "local_db" in a blue badge. Postfach flags display as Ja/Nein. Empty state handled. No mock creditor data remains.
  </done>
</task>

</tasks>

<verification>
1. Open a client with creditors — Gläubiger tab shows real creditor rows
2. All 8 original columns display correctly with real data
3. The 5 v7 columns are visible when scrolling horizontally
4. Creditors with needs_manual_review=true show amber "Ja" badge
5. The address_source column shows "local_db" in a blue badge where applicable
6. Postfach columns show "Ja" (amber) or "Nein" (gray)
7. Client with no creditors shows "Keine Gläubiger vorhanden" empty state
8. The creditor count in the header matches the actual number
9. `cd MandantenPortalDesign && npx tsc --noEmit` — no TypeScript errors
</verification>

<success_criteria>
The Gläubiger tab renders all real creditors with all fields including the 5 v7 fields. Address provenance and Postfach flags are clearly visible. The table scrolls horizontally for the expanded column set. No mock data remains.
</success_criteria>

<output>
After completion, create `.planning/phases/22-client-detail/22-03-SUMMARY.md`
</output>
