---
phase: 26-enhanced-viewer-analytics
plan: 03
type: execute
wave: 2
depends_on: [26-02]
files_modified:
  - server/controllers/adminReviewController.js
  - server/routes/admin-review.js
  - server/models/Client.js
  - MandantenPortalDesign/src/store/api/reviewApi.ts
  - MandantenPortalDesign/src/app/components/review-settings-page.tsx
  - MandantenPortalDesign/src/app/App.tsx
  - MandantenPortalDesign/src/app/components/sidebar.tsx
autonomous: true
requirements: [VIEW-03]

must_haves:
  truths:
    - "Settings page at /review/settings shows confidence threshold number input and auto-assignment toggle"
    - "Settings auto-save on change with confirmation toast"
    - "GET /api/admin/review/settings returns current settings"
    - "PUT /api/admin/review/settings persists updated settings"
    - "Settings are minimal — only confidence threshold and auto-assignment toggle, no extra fields"
  artifacts:
    - path: "server/controllers/adminReviewController.js"
      provides: "getSettings and updateSettings handlers"
      contains: "getSettings"
    - path: "server/routes/admin-review.js"
      provides: "GET /settings and PUT /settings routes"
      contains: "/settings"
    - path: "MandantenPortalDesign/src/app/components/review-settings-page.tsx"
      provides: "ReviewSettingsPage with confidence threshold input and auto-assignment toggle"
      contains: "ReviewSettingsPage"
      min_lines: 60
    - path: "MandantenPortalDesign/src/store/api/reviewApi.ts"
      provides: "getReviewSettings and updateReviewSettings endpoints"
      contains: "useGetReviewSettingsQuery"
    - path: "MandantenPortalDesign/src/app/App.tsx"
      provides: "/review/settings route"
      contains: "review/settings"
  key_links:
    - from: "MandantenPortalDesign/src/app/components/review-settings-page.tsx"
      to: "MandantenPortalDesign/src/store/api/reviewApi.ts"
      via: "useGetReviewSettingsQuery and useUpdateReviewSettingsMutation hooks"
      pattern: "useGetReviewSettingsQuery|useUpdateReviewSettingsMutation"
    - from: "MandantenPortalDesign/src/store/api/reviewApi.ts"
      to: "/api/admin/review/settings"
      via: "RTK Query GET and PUT"
      pattern: "/api/admin/review/settings"
    - from: "server/routes/admin-review.js"
      to: "server/controllers/adminReviewController.js"
      via: "router.get/put('/settings', ...)"
      pattern: "getSettings|updateSettings"
---

<objective>
Create the backend settings endpoints and ReviewSettingsPage for configuring review confidence threshold and auto-assignment toggle.

Purpose: Allow admins to configure review behavior — setting the confidence threshold below which cases require manual review, and toggling automatic case assignment to agents.
Output: Backend settings API + ReviewSettingsPage with auto-save
</objective>

<execution_context>
@/Users/luka.s/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luka.s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-enhanced-viewer-analytics/26-02-SUMMARY.md
@MandantenPortalDesign/src/store/api/reviewApi.ts
@MandantenPortalDesign/src/app/App.tsx
@server/controllers/adminReviewController.js
@server/routes/admin-review.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend settings endpoints + storage schema</name>
  <files>
    server/controllers/adminReviewController.js
    server/routes/admin-review.js
    server/models/Client.js
  </files>
  <action>
**Settings storage approach:** Use a simple document in a `ReviewSettings` collection (or a dedicated top-level document approach). Since there is no ReviewSettings model yet, create one inline or use a simpler approach:

Option A (recommended for simplicity): Store settings in a single MongoDB document using a dedicated `review_settings` collection. Create a minimal Mongoose model inline in the controller (or a small model file if the project convention requires separate model files — check `server/models/` pattern). The collection stores exactly one document with these fields:
- `confidence_threshold`: Number (default: 80, represents percentage 0-100)
- `auto_assignment_enabled`: Boolean (default: false)
- `updated_at`: Date
- `updated_by`: String (admin who last changed)

Actually, since this project uses a single `Client.js` model for most things, and settings are global (not per-client), create a small new model file: `server/models/ReviewSettings.js`:
```javascript
const mongoose = require('mongoose');
const reviewSettingsSchema = new mongoose.Schema({
  confidence_threshold: { type: Number, default: 80 },
  auto_assignment_enabled: { type: Boolean, default: false },
  updated_at: { type: Date, default: Date.now },
  updated_by: { type: String },
}, { collection: 'review_settings' });

module.exports = mongoose.model('ReviewSettings', reviewSettingsSchema);
```

**Controller handlers in `adminReviewController.js`:**

1. `getSettings`:
   - `ReviewSettings.findOne({})` — if null, return defaults `{ confidence_threshold: 80, auto_assignment_enabled: false }`
   - Return: `{ success: true, data: { confidence_threshold, auto_assignment_enabled } }`

2. `updateSettings`:
   - Read `confidence_threshold` and `auto_assignment_enabled` from `req.body`
   - Validate: `confidence_threshold` must be a number between 0-100, `auto_assignment_enabled` must be boolean
   - Use `ReviewSettings.findOneAndUpdate({}, { ...fields, updated_at: new Date(), updated_by: req.user?.email || 'admin' }, { upsert: true, new: true })`
   - Return: `{ success: true, data: { confidence_threshold, auto_assignment_enabled } }`

**Routes in `admin-review.js`:**
- `router.get('/settings', authenticateAdmin, controller.getSettings)`
- `router.put('/settings', authenticateAdmin, controller.updateSettings)`
  </action>
  <verify>
    1. Grep adminReviewController.js for: getSettings, updateSettings, ReviewSettings
    2. Grep admin-review.js for: /settings
    3. Check server/models/ReviewSettings.js exists
  </verify>
  <done>
    - GET /api/admin/review/settings returns current settings (with defaults if none saved)
    - PUT /api/admin/review/settings saves confidence_threshold and auto_assignment_enabled
    - Settings are validated (threshold 0-100, auto_assignment is boolean)
    - ReviewSettings model uses upsert pattern (single document, created on first write)
  </done>
</task>

<task type="auto">
  <name>Task 2: ReviewSettingsPage with auto-save and route wiring</name>
  <files>
    MandantenPortalDesign/src/store/api/reviewApi.ts
    MandantenPortalDesign/src/app/types.ts
    MandantenPortalDesign/src/app/components/review-settings-page.tsx
    MandantenPortalDesign/src/app/App.tsx
    MandantenPortalDesign/src/app/components/sidebar.tsx
  </files>
  <action>
1. **Add types** in `types.ts`:
   ```typescript
   export interface ReviewSettings {
     confidence_threshold: number;
     auto_assignment_enabled: boolean;
   }
   export interface ReviewSettingsResponse {
     success: boolean;
     data: ReviewSettings;
   }
   ```

2. **Add RTK Query endpoints** in `reviewApi.ts`:
   ```typescript
   getReviewSettings: builder.query<ReviewSettingsResponse, void>({
     query: () => '/api/admin/review/settings',
     providesTags: ['ReviewSettings'],
   }),
   updateReviewSettings: builder.mutation<ReviewSettingsResponse, Partial<ReviewSettings>>({
     query: (body) => ({
       url: '/api/admin/review/settings',
       method: 'PUT',
       body,
     }),
     invalidatesTags: ['ReviewSettings'],
   }),
   ```
   Add `'ReviewSettings'` to tagTypes in `baseApi.ts` if not present.
   Export `useGetReviewSettingsQuery` and `useUpdateReviewSettingsMutation`.

3. **Create `review-settings-page.tsx`:**

**Layout:**
- Page wrapper: padding 32px, max-width 600px
- Header: "Review Einstellungen" h1 (24px, font-semibold)
- Single white card (1px #E5E7EB border, 12px radius, padding 24px) containing:

**Confidence Threshold field:**
- Label: "Confidence-Schwellenwert" (14px, font-medium)
- Number input field (use shadcn/ui `Input` with `type="number"`), min 0 max 100, step 1
- Suffix text "%" displayed inline next to the input
- No help text/description — minimal labels only (per user decision)

**Auto-Assignment field:**
- Label: "Auto-Zuweisung" (14px, font-medium)
- Toggle switch (use shadcn/ui `Switch` component from `./ui/switch`)
- No help text/description — minimal labels only (per user decision)

**Auto-save behavior:**
- On confidence threshold change: debounce 500ms, then call `updateSettings` mutation with new value
- On toggle change: immediately call `updateSettings` mutation
- On successful save: show `toast.success('Einstellungen gespeichert')` via sonner
- On error: show `toast.error('Fehler beim Speichern')` via sonner
- Use `useRef` for debounce timer cleanup

**Loading state:** Show Skeleton for input and switch while settings load
**Error state:** Show error message if settings fail to load

4. **Add route** in `App.tsx`: `<Route path="/review/settings" element={<ReviewSettingsPage />} />` — place before `/review/:clientId` route.

5. **Add sidebar link**: Check sidebar for review-related section. If sidebar has sub-navigation for review, add "Einstellungen" link to `/review/settings`. If sidebar component uses a navigation array, add the entry there. Use a Settings/Gear icon from lucide-react.
  </action>
  <verify>
    1. `cd MandantenPortalDesign && npx tsc --noEmit` — no type errors
    2. `cd MandantenPortalDesign && npm run build` — build succeeds
    3. Grep review-settings-page.tsx for: useGetReviewSettingsQuery, useUpdateReviewSettingsMutation, Switch, Input, toast, debounce
    4. Grep App.tsx for: review/settings
    5. Grep reviewApi.ts for: getReviewSettings, updateReviewSettings
  </verify>
  <done>
    - ReviewSettingsPage renders confidence threshold number input and auto-assignment toggle
    - Settings auto-save on change (debounced for number input, immediate for toggle) with toast confirmation
    - /review/settings route is wired in App.tsx
    - RTK Query hooks fetch and update settings via /api/admin/review/settings
    - Design is minimal — only two fields, no extra descriptions (per user decision)
  </done>
</task>

</tasks>

<verification>
1. Backend: adminReviewController.js has getSettings/updateSettings, admin-review.js has /settings routes, ReviewSettings.js model exists
2. Frontend: `cd MandantenPortalDesign && npx tsc --noEmit && npm run build` — zero errors
3. ReviewSettingsPage has exactly 2 fields: confidence threshold number input + auto-assignment toggle
4. Auto-save works: change triggers mutation, success shows toast
5. App.tsx has /review/settings route
</verification>

<success_criteria>
- GET and PUT /api/admin/review/settings endpoints work with confidence_threshold and auto_assignment_enabled
- ReviewSettingsPage shows exactly 2 settings with auto-save and toast feedback
- Page accessible at /review/settings
- Sidebar has link to settings page
</success_criteria>

<output>
After completion, create `.planning/phases/26-enhanced-viewer-analytics/26-03-SUMMARY.md`
</output>
